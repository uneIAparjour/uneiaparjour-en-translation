name: One-off - Cleanup 6 duplicate EN posts

on:
  workflow_dispatch:
    inputs:
      apply:
        description: 'Actually write (unpublish duplicates as draft + update state). Unchecked = dry-run, no writes.'
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  cleanup-duplicate-posts:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: script
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run cleanup
        env:
          WP_URL: ${{ secrets.WP_URL }}
          WP_USERNAME: ${{ secrets.WP_USERNAME }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
        run: |
          ARGS=""
          if [ "${{ inputs.apply }}" = "true" ]; then
            ARGS="--apply"
          fi
          node cleanup-duplicate-posts.js $ARGS

      - name: Commit updated state
        if: ${{ inputs.apply }}
        run: |
          git config user.name "translation-bot"
          git config user.email "actions@github.com"
          git add state/translations.json
          git diff --cached --quiet || git commit -m "Fix state for 6 deduplicated posts [skip ci]"
          git push
